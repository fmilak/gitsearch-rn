import {observer} from 'mobx-react';
import {Box, Text} from 'native-base';
import React from 'react';

const RepoView = observer(() => {
    return (
        <Box>
            <Text>Repo</Text>
        </Box>
    );
});

export default RepoView;
